snippet     ansible_output_with_neline
abbr        debug
options     word
	debug:
		msg: "{{ ${1:result}.stdout.split('\n')[:-1] }}"

snippet     ansible_debug
abbr        debug
options     word
		register: ${1:result}
	
	debug:
		msg: "{{ $1.stdout.split('\n')[:-1] }}"
